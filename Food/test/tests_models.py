# from django.test import TestCase
# import unittest

# from .models import Ingredient, Product, ReceipeIngredient

# class TestIngredient(unittest):

# 	def test_ingredient(self):
# 		ingredient_1 = Ingredient("Bread", 7.3, 48.8, 4.4, 75, 0.19, "2")
# 		ingredient_2 = Ingredient("Ham", 19.1, 1.0, 7.6, 22.5, 0.23, "6")

# 		self.assertEqual(ingredient_1.name, "Bread")
# 		self.assertEqual(player_2.name, "Ham")




# if __name__ == "__main__":
# 	unittest.main()



	# name = models.CharField(max_length=50, unique=True)
	# protein = models.FloatField()
	# carbohydrates = models.FloatField()
	# fat = models.FloatField()
	# quantity_per_portion = models.IntegerField(blank=True)
	# price = models.DecimalField(max_digits=100, decimal_places=2, blank=True)
	# food_type = models.CharField(max_length=2, choices=FOOD_TYPE_CHOICES, blank=True)